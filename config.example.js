// Copy this file to config.js and fill in your Mapbox public token.
// config.js is in .gitignore so the real token never lands in the repo.
// Tokens should be URL-restricted on the Mapbox dashboard
// (https://account.mapbox.com/access-tokens/) to mori-mieru.jp + localhost.
window.MAPBOX_TOKEN = 'pk.eyJ1Ijoi....';
